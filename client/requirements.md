## Packages
lucide-react | Icons for the interface
date-fns | Date formatting
framer-motion | Smooth animations and page transitions
react-hook-form | Form state management
@hookform/resolvers | Zod validation for forms

## Notes
- Tailwind Config requires fontFamily extension for 'display' and 'sans'
- Backend handles mock auth via /api/auth/login with just a username
- Subscription is mocked via /api/auth/subscribe
